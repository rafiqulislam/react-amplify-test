# Node.js with React
# Build a Node.js project that uses React.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    npm install -g @aws-amplify/cli
    amplify pull --appId d3rn4p2tzz99aw --envName local
    npm run build
  displayName: 'npm install and build'
- script: sudo apt-get install -y awscli
  displayName: Install AWS CLI
- script: aws s3 sync ./build s3://pashes3.s3.ap-south-1.amazonaws.com
  displayName: Deploy website
  env: 
  AWS_ACCESS_KEY_ID: AKIA5KZ5SQAIZPMWCR4N
  AWS_SECRET_ACCESS_KEY: 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

